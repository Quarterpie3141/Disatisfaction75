#include "Arduino.h"
#include "Disatisfaction.h"

void setup()
{

pinMode(PB7, INPUT);

pinMode(PC7, OUTPUT);
pinMode(PC6, OUTPUT);
pinMode(PD7, OUTPUT);
pinMode(, OUTPUT);
pinMode(, OUTPUT);
pinMode(, OUTPUT);
pinMode(, OUTPUT);
pinMode(, OUTPUT);
pinMode(, OUTPUT);
pinMode(, OUTPUT);
pinMode(, OUTPUT);
pinMode(, OUTPUT);
pinMode(, OUTPUT);
pinMode(, OUTPUT);
};

void loop()
{



};